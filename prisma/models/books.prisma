model Books {
  id          Int    @id(map: "Book_pkey") @default(autoincrement())
  isbn        String @unique
  title       String
  author      String
  description String
  pageCount   Int
  coverUrl    String

  @@map("books")
  @@schema("public")
}
